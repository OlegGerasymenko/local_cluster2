apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-app-dev1-pvc
  namespace: php-app-dev1
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: manual
  resources:
    requests:
      storage: 100Mi
  volumeName: php-app-logs-dev1
