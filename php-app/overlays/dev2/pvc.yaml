apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-app-dev2-pvc
  namespace: php-app-dev2
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: manual
  resources:
    requests:
      storage: 100Mi
  volumeName: php-app-logs-dev2
